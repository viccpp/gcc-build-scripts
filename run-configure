#!/bin/sh

[ -d build ] || mkdir build
cd build
../gcc-6.3.0/configure --prefix=/opt/gcc-6.3 --enable-languages=c,c++ \
    --disable-multilib --disable-libstdcxx-pch --enable-lto
# --disable-symvers
